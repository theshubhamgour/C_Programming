// Program for addition of two number

#include<stdio.h>
int main(){
    int a,b;
    printf("Enter two number:");

    scanf("%d%d",&a,&b); //& is addres of operator whic take input from user
    printf("The addition of two number is: %d",a+b);
}