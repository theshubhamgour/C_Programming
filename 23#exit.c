// program to demonstrate exit
#include<stdio.h>
void main(){
    for (int i = 0; i < 10; i++)
    {
        if (i==5)
            exit(0); //it terminates the program wherever we use it in the program.
            printf("The number is: %d\n",i);
        
    }
        printf("\nContinue here");
    
}